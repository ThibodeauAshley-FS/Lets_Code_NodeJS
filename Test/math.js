const addition = (x, y) => {
    return sum = x + y;
  };

const subtraction = (x, y) => {
    return sum = x - y;
  };

const multiplication = (x, y) => {
    return sum = x * y;
  };

const division = (x, y) => {
    return sum = x / y;
  };

const sqrt = (x) => {
    return Math.sqrt(64);
  };

const max = (x, y) => {
    return Math.max(x,y);
  };
module.exports = {
    addition,
    subtraction,
    multiplication,
    division,
    sqrt,
    max
}